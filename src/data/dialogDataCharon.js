/**
 * Dialog tracks for the Charon campaign (`currentMissionId: "charon"`).
 * Merged into `dialogTracks` in dialogData.js. Use the same shape as training
 * entries: `say` / `dialogPublicUrl` from `./dialogDataHelpers.js`, `GAME_STATES`
 * from `./gameData.js`.
 */

export const charonDialogTracks = {};
